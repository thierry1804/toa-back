; App-level upload limit is 10M (see DocumentUploadProcessor and friends);
; keep post_max_size/upload_max_filesize above that so PHP never rejects a
; legitimate request that already passed nginx's client_max_body_size.
upload_max_filesize = 15M
post_max_size = 20M
