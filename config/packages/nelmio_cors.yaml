nelmio_cors:
    defaults:
        allow_credentials: true
        allow_origin: ["https://toa-front.dev.arkeup.com"]
        allow_headers: ["Content-Type", "Authorization"]
        allow_methods: ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
        expose_headers: ["Link"]
        max_age: 3600

    paths:
        "^/":
            allow_origin: ["https://toa-front.dev.arkeup.com"]
            allow_headers: ["Content-Type", "Authorization"]
            allow_methods: ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
            allow_credentials: true
            max_age: 3600
